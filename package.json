{
  "name": "project-boilerplate",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces"
  },
  "workspaces": [
    "/api",
    "/site"
  ],
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "dependencies": {
    "dotenv-override": "^5.0.1"
  }
}
