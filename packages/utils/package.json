{
  "name": "utils",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build script'",
    "clean": "rm -rf .cache",
    "patch": "node patch-netlify-toml",
    "prebuild": "run-p patch prefetch",
    "prefetch": "node prefetch-sanity"
  },
  "dependencies": {
    "@sanity/client": "^0.142.0",
    "auth0-js": "^9.11.3",
    "dotenv-override": "^5.0.1",
    "fs-extra": "^8.1.0",
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
