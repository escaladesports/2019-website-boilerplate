{
  "name": "react-escalade-inventory",
  "version": "0.0.0",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/escaladesports/escalade.git",
    "directory": "packages/react-escalade-inventory"
  },
  "scripts": {
    "build": "npx babel src --out-dir dist",
    "clean": "rm -rf dist",
    "dev": "npx babel src --out-dir dist --watch",
    "predev": "npx run-s clean build",
    "prepublishOnly": "yarn run build"
  },
  "peerDependencies": {
    "react": "*"
  },
  "dependencies": {
    "@babel/runtime": "^7.6.0",
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "npm-run-all": "^4.1.5"
  }
}
