{
  "name": "react-escalade-pricing",
  "version": "0.0.0",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/escaladesports/escalade.git",
    "directory": "packages/react-escalade-pricing"
  },
  "scripts": {
    "build": "echo 'No build script'",
    "build:babel": "babel src --out-dir dist",
    "clean": "rm -rf dist",
    "dev": "babel src --out-dir dist --watch",
    "prebuild": "run-s clean build:babel",
    "predev": "run-s clean build:babel",
    "prepublishOnly": "yarn run build"
  },
  "peerDependencies": {
    "react": "*"
  },
  "dependencies": {
    "@babel/runtime": "^7.5.5",
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "npm-run-all": "^4.1.5"
  }
}
