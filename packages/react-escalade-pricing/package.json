{
  "name": "@escaladesports/react-escalade-pricing",
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "run-s clean build:babel",
    "build:babel": "babel src --out-dir dist",
    "clean": "rm -rf dist",
    "dev": "run-s clean build:babel dev:babel",
    "dev:babel": "babel src --out-dir dist --watch",
    "prepublishOnly": "yarn run build"
  },
  "peerDependencies": {
    "react": "*"
  },
  "dependencies": {
    "@babel/runtime": "^7.5.5",
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "npm-run-all": "^4.1.5"
  }
}
